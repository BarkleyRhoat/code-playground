Discussion Questions: Thinking in Types


Objectives
Recall the datatypes in JavaScript
Identify the types of different data
Identify the types of input and output of functions and operators
JS Data Types Review Questions
What are the basic data types in JavaScript? Fill in the table of types below with descriptions of the types and the operators that work on them.
Object is the basic data type that we use to build other, more specialized 'types' of data. Name some of the ways that Object is used in JavaScript (e.g. what other types are really Object underneath).
Exercise - Thinking in Types
When writing a function, it's often helpful to start by considering the inputs and the output. In particular, knowing the data type of the inputs and the data type of the outputs lets you know what kinds of operations are allowed on those arguments. They also hint at what the 'right' operations to do are.
For the following functions, fill in the table with the types of the inputs and the types of the outputs.
Function
types of inputs
type of output
function addFive(number) { return number + 5}; addFive(8)
input = number
output = number + 5 === addFive(8) output would be 13


function sum(A, B) { return A + B }; sum(1, 2)
input = two values
output = a + b ==== sum(1, 2) = 3


function concat(A, B) { return "" + A + B } concat('apple', 'banana')
input = two values (any type)
output a string combining a & b === concat(apple, banana) " applebanana"


document.querySelector(".button")
input = css selector string
output = first matching DOM element. 


window.fetch("myUrlHere") fetch()
input = URL (string or request object specifying the resource to fetch )
output = Response object returning server response


"Shapes" of Objects
Many functions operate on Object types that need to have particular keys. In such cases, it's useful to name the 'shapes' of Object that functions expect. You might say that a 'Person' is an Object that has keys 'name' and 'age'. You might write:
// Person: {
//  name: string,
//  age: number,
// }
to represent the 'shape' of a Person.
When you have functions like the following, you can use the shorthand to write the type of the input and output.
function getName(person) {
  return person.name;
}

function getAge(person) {
  return person.age;
}

function makePerson(name, age) {
  return {
    name: name,
    age: age
  };
}

function birthday(person) {
  person.age += 1;
  return person;
}

// Point: {
// x: number
// y: number
// name: string
//}

function getDistance(pointA, pointB) {
  return Math.sqrt((pointA.x - pointB.x) ** 2 + (pointA.y - pointB.y) ** 2);
}

Fill in the table using the 'shape' shorthand
Function
types of inputs
type of output
const getName = (person) => person.name;
input = person
output = persons name


const getAge = (person) => person,age;
input = person
output = number


const makePerson = (name, age) => ({name, age})
input = name & age
output = person object


const birthday = (person) => (person.age += 1, person)
input = person
output = person age increased by 1 


const getDistance = (pointA, pointB) => Math.sqrt((pointA.x - pointB.x) ** 2 + (pointA.y - pointB.y) ** 2);
input = pointA & pointB objects
output = number representing distance between pointA and pointB


Function Signatures
It's often conventional to write the 'signature' of a function to concisely capture the function's name, the names and types of the arguments, and the type of the return value. For the getDistance function above, the signature might be written as:
// getDistance(pointA: Point, pointB: Point): number
The : is used to indicate that the type is 'about' the preciding name. In pointA: Point, pointA is a Point.
Reading this function signature out loud, you would say
getDistance is a function that takes in Points pointA and pointB and returns a number.
For practice, add the function signature to the functions in the table.
Function
types of inputs
type of output
function signature

getName  
input = person 
output = string
signature = getName(person: Person): string



getAge 
input  = person
output = string
signature = getAge(person: Person): number



makePerson 
input = string, number
output = person
signature = makePerson(name: string, age: number): Person



birthday
input = person
output = person
signature = birthday(person: Person): Person



getDistance
input = point, point
output = number (distance between 2 points)
signature = getDistance(pointA: Point, pointB: Point): number



Operators as 'Functions'
You can think of many operators as if they were functions. They take in inputs and evaluate to an output.
For the following operators, fill in the types of the inputs and outputs. Following the example, try to write out a function signature, as if the operator was a function.
Operator
types of inputs
type of output
function signature

*
input = number, number
output = number
signature = multiply(a: number, b: number): number

+ (string)
input = string, string
output = string
signature = add(a: string, b: any): string


+ (number)
input = number, number
output = number
signature = add(a: number, b: number): number


-
input = number, number
output = number
signature = subtract(a: number, b: number): number


^
input = number, number
output = number
signature = xor(a: number, b: number): number



**
input = number, number
output = number
signature = power(base: number, exponent: number): number


\
input = number, number
output = number
signature = divide(a: number, b: number): number


&&
input = boolean, boolean 
output = boolean 
signature = boolean(a: boolean, b: boolean): boolean



Functions that take in functions as arguments
Some functions take in functions as arguments ('callback functions'). If those functions will be called with particular types of arguments, it's helpful to specify the type of function that will be passed in.
Note: you may see the word void meaning 'this function does not return anything'
Fill in the rest of the table.
Function
types of inputs
type of output
function signature

EventTarget.addEventListener
input = string, handler(e: Event): void
output = void
signature = addEventListener(type: string, handler: function): void

window.setTimeout
input = callback(): void, number
output = number
signature = setTimeout(callback(): void, delay: number): number

Array.prototype.map
input = callback(item: T): U 
output = U[]
signature = map(callback(item: T): U): U[]

Array.prototype.find
input = callback(item: T): boolean
output = T
signature = find(callback(item: T): boolean): T

Array.prototype.filter
input = callback(item: T): boolean
output = T[]
signature = filter(callback(item: T): boolean): T[]


Array.prototype.reduce
input = callback(acc: U, item: T): U, U
output = U
signature = reduce(callback(acc: U, item: T): U, initialValue: U): U

